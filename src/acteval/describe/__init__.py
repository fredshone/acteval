"""Matplotlib visualisation helpers for activity schedule distributions.

This package is not imported by the evaluation pipeline — it is intended for
interactive / notebook use to visually inspect and compare populations.

Sub-modules:

- ``frequency``   — stacked bar plots of activity density over the day
- ``times``       — histogram grids of start times, end times, and durations
- ``transitions`` — sequence probability waterfall plots
"""
